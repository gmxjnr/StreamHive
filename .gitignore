# Environment / secrets
.env

# Dependencies
/vendor

# Uploaded video files (keep the folder, ignore its contents)
/public/uploads/*
!/public/uploads/.gitkeep

# OS / editor noise
.DS_Store
Thumbs.db
